/* =========================================================================
   AS 시스템 설정 파일

   ★ 이 파일은 처음 한 번만 채우면 됩니다.
     앞으로 admin.html / contractor.html 을 새 버전으로 바꿔도
     이 파일은 그대로 두세요. (덮어쓰지 마세요)
   ========================================================================= */

window.AS_CONFIG = {

  // 1) Apps Script 웹 앱 주소
  //    Apps Script 화면 → 배포 → 배포 관리 에 있는 주소를 따옴표 안에 붙여넣으세요.
  //    끝이 /exec 로 끝나야 합니다.
  apiUrl: 'PASTE_YOUR_APPS_SCRIPT_WEB_APP_URL_HERE',

  // 2) 평면도 이미지가 들어 있는 폴더
  //    admin.html 과 같은 위치에 images 폴더가 있으면 그대로 두시면 됩니다.
  planDir: 'images/',

  // 3) 관리자 화면 자동 로그아웃 시간(분). 0 으로 두면 자동 로그아웃하지 않습니다.
  idleLogoutMinutes: 30

};
