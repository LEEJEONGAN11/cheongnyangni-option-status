/* ─────────────────────────────────────────────────────────────
   이 파일 하나만 직접 고치시면 됩니다.
   나머지 파일(index.html, contract.html, electric.html, data.js …)은
   새 버전을 받을 때마다 그냥 덮어쓰셔도 이 설정은 그대로 남습니다.

   아래 큰따옴표 안에 Apps Script 웹 앱 주소를 붙여넣으세요.
   (Apps Script → 배포 → 새 배포 → 웹 앱 → 나온 주소)

   주소를 안 넣어도 사이트는 그냥 열립니다. 다만 "수정"만 안 됩니다.
   ───────────────────────────────────────────────────────────── */
window.WYC_CONFIG = {
  apiUrl: "여기에_APPS_SCRIPT_주소를_붙여넣으세요",

  // 전기 도면을 넣어둘 폴더와, 한 세대에서 몇 장까지 찾아볼지
  electricPlanDir: "images/전기/",
  electricPlanMax: 8
};
