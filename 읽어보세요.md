# 사이트 ② 옵션 정리 (계약 현황 · 전기공사)

이 폴더는 **현장 관리 사이트와 다른 저장소**에 올리시면 됩니다.
서로 파일을 안 건드립니다.

| 화면 | 파일 | 내용 |
|---|---|---|
| 처음 화면 | `index.html` | 둘 중 고르기 |
| 호수별 옵션 계약 현황 | `contract.html` | 155세대 · 19개 항목 · 항목별 PDF |
| 전기공사 옵션 | `electric.html` | 7품목 · 34세대 · 세대별 전기 도면 |

---

## 고칠 파일은 `settings.js` 하나

```js
window.WYC_CONFIG = {
  apiUrl: '여기에 옵션용 Apps Script 주소',   // ← 이 줄만
  ...
};
```

**비워두셔도 됩니다.** 표는 그냥 잘 보이고, "수정" 기능만 안 켜집니다.

---

## 수정 기능 켜기 (선택)

현장 관리 시스템과는 **완전히 별개**입니다. 그쪽은 안 건드립니다.

1. 구글시트를 새로 하나 만들고, 주소의 긴 문자열(시트 ID)을 복사
2. [script.google.com](https://script.google.com) → 새 프로젝트 → `apps-script/Options.gs` 붙여넣기
3. 맨 위 `setup()` 안의 **시트ID · 수정비밀번호** 두 줄을 채우고 `setup` 실행
4. **배포 → 새 배포 → 웹 앱** (실행: **나** / 액세스: **모든 사용자**)
5. 나온 주소를 `settings.js` 의 `apiUrl` 에 붙여넣고 커밋

그러면 오른쪽 위에 `✏️ 수정하기` 가 생깁니다.
세대를 눌러 **수정** 탭에서 체크하고 저장하면 다른 사람 화면에도 반영됩니다.
잘못 고쳤으면 **원본으로 되돌리기** 한 번이면 됩니다.

서버에는 **바뀐 것만** 저장됩니다. 원본 155세대 데이터는 `data.js` 안에 들어 있어서
서버가 죽어도 표는 그대로 보입니다.

---

## 전기 도면 넣기

`images/전기/` 폴더에 올리기만 하면 자동으로 잡힙니다.

- 타입별: `59C_1.jpg`, `59C_2.jpg` … (한 세대당 8장까지)
- 그 세대만 다르면: `102_1902_1.jpg` (동_호수_번호 — 타입 파일보다 우선)
- jpg · png · webp 다 됩니다

탭 이름은 처음엔 "도면 1, 도면 2…" 인데, 세대를 열고 **수정 → 도면 이름** 에서
"조명", "콘센트" 처럼 바꾸면 그 타입 전체에 적용됩니다.

---

## 안 될 때

| 증상 | 원인 |
|---|---|
| "보기 전용" | `settings.js` 가 비어 있음 (정상. 수정만 안 됨) |
| "서버 연결 실패" | 배포할 때 액세스 권한을 "모든 사용자"로 안 함 |
| 글씨만 나오고 디자인 없음 | `style.css` 가 안 올라감 |
| 평면도가 안 보임 | `images` 폴더가 통째로 안 올라감 |

---

## 데이터 출처

- 옵션 계약 현황: 옵션 계약 현황 시트 + 옵션 집계표 (2026-07-07, 교차 검증 완료)
- 전기공사 옵션: 단위세대 정리.xlsx 의 **입면정리** 시트 (2026-08-18)
  - 7개 품목 · 시공 대상 34세대 · 연 59건
